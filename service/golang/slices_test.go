package main

/*import "golang.org/x/tour/pic"

func Pic(dx, dy int) [][]uint8 {
	if dx < 1 || dy < 1 {
		return nil
	}
	y := make([][]uint8, dy)
	for i := range y {
		x := make([]uint8, dx)
		y[i] = x
		for j := range x {
			// 几个有趣的函数包括 (x+y)/2, x*y, x^y, x*log(y) 和 x%(y+1)。
			y[i][j] = uint8(i ^ j)
		}
	}
	return y
}

func main() {
	pic.Show(Pic)
}*/
